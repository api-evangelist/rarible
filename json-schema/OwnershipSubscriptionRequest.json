{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OwnershipSubscriptionRequest",
  "type": "object",
  "properties": {
    "@type": {
      "type": "string",
      "enum": [
        "OWNERSHIP"
      ]
    },
    "id": {
      "$ref": "#/components/schemas/OwnershipId"
    }
  },
  "required": [
    "@type",
    "id"
  ]
}