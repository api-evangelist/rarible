{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OrderUpdateEvent",
  "type": "object",
  "properties": {
    "@type": {
      "type": "string",
      "enum": [
        "UPDATE"
      ]
    },
    "order": {
      "$ref": "#/components/schemas/Order"
    }
  },
  "required": [
    "@type",
    "order"
  ]
}